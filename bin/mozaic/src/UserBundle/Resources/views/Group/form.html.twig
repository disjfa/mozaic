{% extends 'glynn-admin/base.html.twig' %}

{% block title %}
    Create user - {{ parent() }}
{% endblock %}

{% block body %}
    <h1>Create user</h1>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-primary">
        <i class="fa fa-floppy-o"></i>
        Save
    </button>
    {{ form_end(form) }}
{% endblock %}


